Place the hero image for the Coming Soon page here as:

    wc-delhi-hero-image.webp

Referenced by `coming-soon.html` (`.hero-photo`). Recommended size: ~1500×1060px
(landscape, ~1.41:1) to match the `<img>` width/height attributes already set in
the markup — update those attributes if the real file's dimensions differ.
